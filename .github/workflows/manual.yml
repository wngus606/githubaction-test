name: Protobuf Build Warning
on:
  workflow_dispatch:
jobs:
  protoc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          ref: main
      - name: protoc build
        run: |
          ls -al
          protoc -v
